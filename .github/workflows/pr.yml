name: build-docker-push
on: 
  pull_request:
    branches:
      - 'master'
    
jobs:
  generateInputPaths:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Testing
      run: echo " This is Master Branch"
